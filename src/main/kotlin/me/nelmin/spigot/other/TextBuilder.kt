package me.nelmin.spigot

class TextBuilder(private val text: String) {
    private var updatedText = text

    fun append(string: String, position: Int): TextBuilder {
        if (position )

        return this
    }

}